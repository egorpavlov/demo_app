<%= form_for(@userr) do |f| %>
  <% if @userr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userr.errors.count, "error") %> prohibited this userr from being saved:</h2>

      <ul>
      <% @userr.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
